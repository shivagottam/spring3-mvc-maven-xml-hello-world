
tasks:
  unit-test:
    runner: razorci/openjdk:11-buster
    steps:
    - checkout
    - commands:
      - |
        mvn clean package
        ls -a 
        ls target
        scp -r target/spring3-mvc-maven-xml-hello-world-1.2.war ubuntu@13.233.252.53:/opt/apache-tomcat-8.5.81/webapps
